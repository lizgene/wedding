(function(){
  'use strict';

  describe('Service: Guest Factory', function () {
    // beforeEach(module('app.services'));

    // it('returns a JSON object with guests', inject(function ($resource, Guest) {
    //   var guests;
    //   $resource.when('query', '/api/guests').respond({guests: ["guest 1", "guest 2"]}, {});
    //
    //   var guestList = Guest.query().success(function(response){ guests = response.guests; });
    //   expect(guests).not.toBeDefined();
    //
    //   $httpBackend.flush();
    //   expect(guests).toEqual(["guest 1", "guest 2"]);
    // }));
  });
})();
