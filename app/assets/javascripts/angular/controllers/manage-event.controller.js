angular.module('app.controllers').controller('manageEventCtrl',
  ['Invitation', 'Guest', '$stateParams',
  function(Invitation, Guest, $stateParams) {

    //this will take a stateParams and get an event based on id (assuming multiple events in futue)
}]);
